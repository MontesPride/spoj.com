//  	2015-01-26 16:56:30

#include <stdio.h>

int main()
{
    int x;
    int n,a;
    int *wsk;
    scanf("%i",&x);
    for(int i=0;i<x;i++)
    {
        scanf("%i",&n);
        wsk = new int [n];
        for(int j=0;j<n;j++)
        {
            scanf("%i",&a);
            wsk[j]=a;
        }
        for(int j=1;j<n;j++)
        {
            printf("%i ",wsk[j]);
        }
        printf("%i \n",wsk[0]);
    }
    return 0;
}
